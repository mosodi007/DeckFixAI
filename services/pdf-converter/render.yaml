services:
  - type: web
    name: pdf-converter
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3001
    healthCheckPath: /health

